# Media Folders

Add your personal files here:

- `photos/`: `.jpg`, `.jpeg`, `.png`, `.webp`
- `gifs/`: `.gif`
- `voice_notes/`: `.ogg`, `.oga`, `.mp3`, `.m4a`
- `videos/`: `.mp4`, `.mov`, `.webm`

The bot will occasionally choose one random media file after sending a romantic message.
